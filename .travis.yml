# GAE auto-deploy
deploy:
  provider: gae
  keyfile: "GroupAPI-1223f397926e.json"
  project: "groupapi-249215"
install:
  - sudo pip install -r requirements.text
  - sudo bash configurelib.sh
